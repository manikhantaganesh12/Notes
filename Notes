find -name filename.txt
grep "text" filename.ext
grep -R "text" /folder
file filename ... determines the filetype
su user2 ... stays in our home directory
su -l user2  ... takes to the user2 home directory

rwx 421

/etc - system files of OS ... passwd, shadow, sudoers
/var - files which are frequently accessed/written by services or applications and other data not necessarily
associated with a specific user... e.g. /var/log , 
/root - home directory of the root user

/tmp - any user has access to this folder - contents are cleared once the system restarts
Nano - ctrl+6 , alt+6, alt + u -- select text, copy text and paste text

wget "Filepath" - for downloading the file
scp file_to_be_copied user@remoteip:destination/filename
scp user@remoteip:source_with_file filename

to convert into a web server for sharing files  - python3 -m http.server
wget http://10.129.162.81:8000/myfile - the http server runs on port 8000

Check this alternative for python web server - https://github.com/sc0tfree/updog

ps ... list of processes
ps aux ... list of processes run by other users and those that do not run from a session (e.g. system processes)

top ... gives the real-time statistics of the processes and auto-refreshes every 10 seconds

kill PID

SIGTERM - Kill the process, but allow it to do some cleanup tasks beforehand
SIGKILL - Kill the process - doesn't do any cleanup after the fact
SIGSTOP - Stop/suspend a proces

systemctl start/stop/enable/disable/status apache2 - for starting the service at bootup


& or Ctrl+Z for backgrounding a process...
fg for bringing the backgrounded process to forefront...


Crontab help - https://crontab-generator.org/ , https://crontab.guru/

crontab -e ... to edit the crontab

/etc/apt/sources.list - list of repositories

Bash Scripting - https://tryhackme.com/room/bashscripting
Regular Expressions - https://tryhackme.com/room/catregex


C:\windows
%windir%  - system environment variable for windows directory

most of the tools reside in \windows\system32 folder
C:\users\"Username"  - home folder
lusrmgr.msc - local user and group management

msconfig - system configuration utility - diagnosing startup issues
taskmgr - to manage (enable/disable) starup items

msconfig - system configuration - services - list of all services (running or stopped)

shared folder - computer management - shared folder - shares

findstr - similar to grep
A - Z index of commands in windows cmd - https://ss64.com/nt/

Further reading material for windows -

Antimalware Scan Interface - https://docs.microsoft.com/en-us/windows/win32/amsi/antimalware-scan-interface-portal
Credential Guard - https://docs.microsoft.com/en-us/windows/security/identity-protection/credential-guard/credential-guard-manage
Windows 10 Hello - https://support.microsoft.com/en-us/windows/learn-about-windows-hello-and-set-it-up-dae28983-8242-bb2a-d3d1-87c9d265a5f0#:~:text=Windows%2010,in%20with%20just%20your%20PIN.
CSO Online - The best new Windows 10 security features - https://www.csoonline.com/article/3253899/the-best-new-windows-10-security-features.html

Group Policy Management for GPOs

GPOs are distributed to the network via a network share called SYSVOL, which is stored in the DC.
All users in a domain should typically have access to this share over the network to sync their GPOs periodically. 
The SYSVOL share points by default to the C:\Windows\SYSVOL\sysvol\ directory on each of the DCs in our network.



gpupdate /force for updating the gpo immediately on to the system

https://tryhackme.com/room/activedirectoryhardening
https://tryhackme.com/module/hacking-active-directory


CMD.exe commands 
set ... to view the path
ver ... OS version
systeminfo

ping
tracert
nslookup domain 1.1.1.1

cd - displays current folder - where am i
dir /a - Displays hidden and system files as well.
dir /s - Displays files in the current directory and all subdirectories.

mkdir foldername
rmdir foldername

type filename ... similar to cat filename in linux... details of the file directly on to the terminal or 
more filenaam.txt can also be used to read the contents

copy test1.txt test2.txt ... for copying the files
move file location ... for moving the files


tasklist
taskkill

tasklist /FI "imagename eq notepad.exe"  filter for notepad

Powershell:
Get-Command -Name "Somethin*"
Get-Help "cmdletname"
Get-Alias


Find-Module -Name "Name"
Install-Module -Name "Name"
Select-String ... similar to grep... also supports regex

Get-ChildItem | Where-Object -Property Length -gt 100  ... items in the current folder wiht size gt 100


echo $SHELL   ...details of the shell
cat /etc/shells   ... details of all the shells on the system

type the shell name to change the shell ... e.g. zsh

to change the shell permanently ... chsh -s /usr/bin/zsh

Private IP - 
10.0.0.0 - 10.255.255.255 (10/8)
172.16.0.0 - 172.31.255.255 (172.16/12)
192.168.0.0 - 192.168.255.255 (192.168/16)


https://tryhackme.com/room/networksecurityprotocols?vccr=1  ... Complete this later
https://letsencrypt.org/

The ~/.ssh folder is the default place to store these keys for OpenSSH.

The authorized_keys file in this directory holds public keys that are allowed access to the server
if key authentication is enabled.




